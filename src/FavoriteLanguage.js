import React from 'react'

const FavoriteLanguage = ({language}) => {
  // ES6 destructuring
  return (
    <div>
        <span>and their favorite language is {language}</span>
    </div>
  )
}

export default FavoriteLanguage;